//Here are some common styles to name bindings:
    //a) camelCase (Canonical JS)
    //b) PascalCase
    //c) snake_case
    //d) UPPER_SNAKE_CASE
    //e) kebab-case (illegal in JS)
    //f) flatcase
    //g) dot.case (illegal in JS)
    //h) Train-Case (illegal in JS)
    //i) StudlyCaps
    //j) HungarianNotation

//NOTE: Some bindings (Constructors) would be PascalCase in JS.