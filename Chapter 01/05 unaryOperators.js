//Arithmetic Operators mentioned prior are binary, i.e., they are applied to 2 values.
//But some operators can be applied to a single value:

//a) typeOf:
console.log(typeof 2);
console.log(typeof 'str');
//b) minus (not subtract):
let x = -2;
console.log(x);